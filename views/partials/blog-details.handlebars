<section class="post">
  <a href="/blog/{{id}}">
    <header class="blog-header">
      <h2>{{title}}</h2>
      <p class="blog-details">Posted by
        {{user.username}}
        on
        {{created_date}}</p>
    </header>
    </a>
    <div class="blog-text"><p>{{text}}</p></div>
  
</section>